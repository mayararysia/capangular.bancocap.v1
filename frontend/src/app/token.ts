import { InjectionToken } from '@angular/core';
import { IConfig } from 'ngx-mask';

export const AppConfig = new InjectionToken<any>('');

// const maskConfig: Partial<IConfig> = {
//     validation: false,
//   };
